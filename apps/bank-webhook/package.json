{
  "name": "bank-webhook",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn esbuild ./src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js",
    "start": "yarn node dist/index.js",
    "dev": "yarn build && yarn start"
  },
  "dependencies": {
    "@repo/db": "*",
    "@types/express": "^5.0.6",
    "esbuild": "^0.28.0",
    "express": "^5.2.1"
  }
}
